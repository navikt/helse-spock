apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: spock-db-policy
  namespace: tbd
spec:
  egress:
    - to:
        - ipBlock:
            cidr: 35.228.145.103/32
  podSelector:
    matchLabels:
      dbPolicyUser: spock-opprydding-dev
  policyTypes:
    - Egress
